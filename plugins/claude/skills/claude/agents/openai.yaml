interface:
  display_name: "Claude"
  short_description: "Ask Claude Code anything from Codex"
  default_prompt: "Use $claude to get a second opinion on the change I just made."
  brand_color: "#6B4FBB"

policy:
  allow_implicit_invocation: true
